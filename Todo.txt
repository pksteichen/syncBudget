================================================================================
  BudgeTrak — Outstanding Tasks (v2.3)
================================================================================

[x] TTL Policies
    ─────────────
    TTL policy configured for pairing_codes/expiresAt.
    Groups use lastActivity for TTL — already set up.

[ ] Cloud Function for Subcollection Cleanup (optional, requires Blaze plan)
    ─────────────────────────────────────────────────────────────────
    When a group document is deleted by TTL, its subcollections are
    NOT auto-deleted. The app's dissolveGroup() now deletes all 13
    subcollections + Cloud Storage receipts, but TTL-triggered
    deletion doesn't run app code.

    Deploy a Cloud Function to clean up orphaned subcollections.
    Optional — orphaned subcollections are encrypted and cost little.

    Requires: Blaze plan + Firebase CLI (npm install -g firebase-tools)

[x] Expired Pairing Code Cleanup
    ─────────────────────────────
    TTL policy on pairing_codes/expiresAt handles this automatically.

[x] Migrate Encryption Key to EncryptedSharedPreferences
    ─────────────────────────────────────────────────────
    Done — SecurePrefs.kt wraps EncryptedSharedPreferences.
    GroupManager reads key from SecurePrefs with plain prefs fallback.

[ ] Remaining Audit Items (LOW priority)
    ─────────────────────────────────────
    21 LOW + 5 INFO items from v2.3 audits. See audit files in
    /Download/BudgeTrak/support/AUDIT_*.txt for details.
    Key items:
    - budgetToday derivedStateOf won't re-trigger on midnight rollover
    - Integrity check reads ALL Firestore docs every 5 min (cost at scale)
    - Duplicate backfill_linked_amounts v1/v2 migrations
    - Unstructured CoroutineScope in dialog photo delete

[ ] Update Firestore Security Rules
    ────────────────────────────────
    Deploy updated rules from /Download/firestore_rules.txt:
    - Removed nextDeltaVersion requirement from group create
    - Added all 8 sync collections (transactions, categories, etc.)
