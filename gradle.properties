org.gradle.jvmargs=-Xmx2048m -Dfile.encoding=UTF-8
android.useAndroidX=true
kotlin.code.style=official
android.nonTransitiveRClass=true

# Termux-specific aapt2 override (ARM-native binary from local Android SDK)
# lives in ~/.gradle/gradle.properties on dev machines that need it. CI builds
# on x86_64 Linux runners use AGP's bundled aapt2 transparently.
